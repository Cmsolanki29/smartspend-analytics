# Dev: leave REACT_APP_API_URL unset — the app calls same-origin /api and setupProxy.js forwards to :8002.
# Only set this for production builds (npm run build) when the API is not co-hosted:
# REACT_APP_API_URL=http://127.0.0.1:8002/api
