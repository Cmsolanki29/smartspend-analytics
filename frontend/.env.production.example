# Production build (npm run build) — set on your host (Vercel, Railway, etc.)

# Required: your deployed FastAPI URL (must include /api)
REACT_APP_API_URL=https://api.your-domain.com/api

# Optional admin unlock for diagnostics build
# REACT_APP_ADMIN_TOKEN=change-me
